# MovieNewsIOS

This folder contains the Swift iOS implementation.

Open the package in Xcode:

```bash
open Package.swift
```

Then choose an iOS simulator and run `MovieNewsIOS`.

The source is organized with VIPER boundaries in `MoviesVIPER.swift`, `NewsVIPER.swift`, `Entities.swift`, and `Services.swift`.

